interface:
  display_name: "Bee Evolving"
  short_description: "Run bee's gated self-improvement loop over its collected feedback digest. Use when the human asks bee to improve itself from ranked friction/feedback — in the bee repository only, on the human's explicit invocation. Never auto-runs, never runs in a host repo, never pushes on its own."
policy:
  allow_implicit_invocation: true
